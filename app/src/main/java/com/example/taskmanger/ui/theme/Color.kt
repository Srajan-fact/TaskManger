package com.example.taskmanger.ui.theme

import androidx.compose.ui.graphics.Color

// ── Neon Dark Color System (SOP v1.0) ──────────────────────────
val Background    = Color(0xFF1C1C1C)   // Primary screen background
val SurfaceColor  = Color(0xFF2A2A2A)   // Cards, containers, elevated surfaces
val SurfaceInput  = Color(0xFF232323)   // Input fields, search bars
val BorderColor   = Color(0xFF3A3A3A)   // Card borders, input outlines, dividers
val Accent        = Color(0xFFC8FF00)   // Neon green — headlines, CTAs, active states
val AccentDim     = Color(0xFF8AAB00)   // Disabled / secondary accent
val TextPrimary   = Color(0xFFFFFFFF)   // Body text, card titles, list labels
val TextSecondary = Color(0xFF888888)   // Subtitles, captions, placeholders
val TextOnAccent  = Color(0xFF000000)   // Text placed ON the neon accent
val ColorPositive = Color(0xFFC8FF00)   // Success / positive delta
val ColorNegative = Color(0xFFFF4D4D)   // Errors, alerts, negative values
val ColorNeutral  = Color(0xFF888888)   // Inactive / disabled elements
